{
    "ConnectionStrings": {       
        "DefaultConnection": "Server=localhost;Database=RaphaelDB;Trusted_Connection=True;TrustServerCertificate=True;"
    },
    "Jwt": {
        "//Key": "Empty on purpose: this file is committed to a public repository. Set it once per machine with: dotnet user-secrets set \"Jwt:Key\" \"<a long random value>\" --project Raphael.Api. The API refuses to start without it.",
        "Key": "",
        "Issuer": "Raphael.Auth",
        "Audience": "Raphael.Client",
        "ExpiresInMinutes": 600
    },
    "Diagnostics": {
        "//LogSensitiveData": "Writes SQL parameter values into the logs. Those values are patient names, addresses, telephone numbers and signatures, which §3 of the constitution forbids in logs — so this stays false in every file that is committed. Turn it on for a diagnosis, on your own machine, against a database whose contents you are willing to read in a log, and turn it off afterwards. It is a key of its own, and not the environment being called Development, because the Azure environment is called Development too and holds real patients.",
        "LogSensitiveData": false
    },
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*"
}

